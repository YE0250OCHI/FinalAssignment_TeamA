# JOB設計

## 概要

- JOBは、スマホからの出庫依頼、自動倉庫からの入庫要求を、アプリの処理単位としてまとめたデータのことである。
  - 出庫JOB：スマホからの出庫依頼を実行するためのJOB
  - 入庫JOB：自動倉庫からの入庫要求を受けて実行するためのJOB
- 各自動倉庫は、出庫JOBと入庫JOBは独立して実行でき、サーバーはこれを分けて配信する。
- 出庫JOBは、JOB生成に成功したあと、要求にある品種と同一商品およびそれを保管する自動倉庫をJOBに紐づけ、対象の自動倉庫へ配信する。
  - 主にスマホからの指示を直接送信するプッシュ型配信と、自動倉庫からの問い合わせに応じて指示を返送するプル型配信を行う。
  - 出庫JOBの進捗途中に異常が発生した際は、商品および自動倉庫の再割当を試行する。
- 入庫JOBは、自動倉庫から入庫要求を受けて、その要求にある品種に対して個別IDを振り、JOBを作成して自動倉庫に続行させる。
- JOBは進捗状態をもち、これを管理する。
  - 一部の進捗状態には時限を設定し、規定時間内に次の状態へ遷移しているかを監視する。
  - 時限付き状態が、規定時間を超えても状態遷移をしなかった場合はタイムアウト（異常）として処理される。
- 異常発生時は、JOBの進捗を中断し、復旧操作を行う。
  - 復旧可能なものは、通常と同じフローで処理が続行される。
  - 復旧不可能なものは、異常終了扱いとなる。

## ライフサイクル

1. スマホの出庫依頼、自動倉庫の入庫要求からJOBが作成される
2. 自動倉庫にJOBが配信される
3. 自動倉庫からJOBの進捗報告を受信し、JOB状態を遷移する
4. JOBが最終状態まで遷移したものは履歴扱いとなる
    - 正常に最後まで遷移したものは完了
    - 異常復旧できなかったものは異常終了
    - キャンセルされたものはキャンセル済み

## JOB作成

- 出庫JOBは、スマホからの出庫依頼によって作成される
- 入庫JOBは、自動倉庫からの入庫要求によって作成される
- JOB作成は１つのトランザクション中で行い、状態の不整合がでないようにする
  - このトランザクション中に、割り当てロジックは入れない（作成のみを感心ごととする）

### JOBの採番と作成

JOB番号の採番に競合しないように、1トランザクション中に採番を行う。

1. 各種端末から、出庫依頼や入庫要求を受信する
2. トランザクションを開始
3. 品種コードの検証（UPDLOCK、HOLDLOCK）
4. そのJOBが作成可能か？
    - 出庫：在庫確認
    - 入庫：空き容量確認、入庫JOB競合
5. 入庫JOBの場合は、商品IDの採番を試みる
6. 当日の作成済みJOBをロック付きで検索
7. 最大連番を取得する
8. 最大連番＋1でJOB番号を作る
9. その他必要情報を埋め、UnassignedでJOBデータを作る
10. JOBをINSERTする
11. コミット

コミット前に競合した場合
- 後続のJOB作成処理は、当日のJOB一覧の読み取り時にロック解除待機状態となる
- ロック解除後、先行処理で作成されたJOBを含めた状態で最大連番を取得できる
- 取得した最大連番をもとに、次のJOB番号を採番して処理を継続する

コミット後に競合した場合
- 後続のJOB作成処理は、更新後のJOB一覧を通常通り取得する
- 取得した最大連番をもとに、次のJOB番号を採番して処理を継続する

補足
- JOB番号はPKなので、JOB番号が重複するとINSERTに失敗する
- ただし、上記の競合回避策により通常発生しない事象である
- したがって、INSERT失敗した場合は、処理実装の不具合として、例外エラーとする

## 商品ID採番ロジック

- 入庫JOBでは、品種コードから商品IDの採番を行う。
- 複数の自動倉庫から入庫要求が来た場合、採番ロジックが競合する可能性がある。
- 最新連番の取得->連番更新を1トランザクションで行い、処理が競合しないようにする。

1. （作成ロジックからトランザクション継続中）
2. 品種コードに対応する品種マスタ行を、ロック付きで取得する（UPDLOCK）
3. 採番日時と最新連番を取得する
4. 採番日時が今日の日付と異なる場合は1、同じ場合は最新連番+1を次連番とする
5. 品種マスタの最新連番を次連番、採番日時を現在日時に更新する
6. 採番した商品IDを作成ロジックへ返す

コミット前に競合した場合
- 後から来た入庫要求側が、データ読みとり時点でロック解除待機状態となる
- ロック解除後、更新後の最新連番・採番日時を取得し、処理を継続する。

コミット後に競合した場合
- 後から来た入庫要求側が、更新後の最新連番・採番日時を通常通り取得し、処理を継続する。

## 出庫JOBの配信

出庫JOBの配信は、以下の2方式が存在する

### プッシュ配信（サーバー -> 自動倉庫）

スマホ操作やタイムアウトを起点とするイベントで、自動倉庫への指示を行う目的で利用する。

- スマホからの出庫依頼を受信し、出庫JOBの作成に成功したとき
- 状態遷移タイムアウトによりJOBが中断され、代替の自動倉庫が決定されたとき
- 自動倉庫の異常報告によりJOBが中断され、代替の自動倉庫が決定されたとき
- 作業中の自動倉庫からのオンライン要求を受信してJOBが中断され、代替の自動倉庫が決定されたとき
- プッシュ配信がタイムアウトし、代替の自動倉庫が決定されたとき

### プル配信（自動倉庫 -> サーバー -> 自動倉庫）

自動倉庫からのアクションを起点とするイベントで、自動倉庫への指示を行う目的で利用する。

- 未割当JOBが存在し、自動倉庫の定時間JOB要求を受信したとき
- 未割当JOBが存在し、自動倉庫からのオンライン要求を受信したとき
- 自動倉庫からの入庫依頼を受信し、入庫JOBの作成に成功したとき

### 出庫JOB配信の手順

- プッシュ型配信とプル型配信では、起点タイミングが異なるので競合する可能性がある。
- JOBデータの読み取り->状態更新を1トランザクションで行い、処理が競合しないようにする。

1. トランザクションを開始
2. 配信相手の自動倉庫が、JOBを持っていないか確認する
3. 未割当JOBのうち、最も登録の古いJOBを取得する
4. JOBをAssignedに更新する
5. 商品をReservedに更新する
6. 自動倉庫に実行中JOB番号を設定する
7. コミット

コミット前に競合した場合
- 後から来た配信サービス側が、JOBの割当の更新時にロック解除待機となる
- ロック解除後に、UPDATEの条件から外れるため、更新レコード数が0件となる＝更新失敗　-> ロールバックを実行
- 更新失敗を受けて「JOBなし」を返す（またはプッシュを行わない）

コミット後に競合した場合
- 自動倉庫がJOBを持っていることを確認して処理を終了する　-> ロールバックを実行
- 「JOBなし」を返す（またはプッシュを行わない）

### プッシュ配信のタイムアウト

- プッシュ配信は、レスポンス応答時間に時限をつける
- 時限を超過したら、HTTP通信のタイムアウトとして処理する

- JOBに割り当てた、

## JOB状態

|id|name|備考|
|:---|:---|:------|
|1|Unassigned|JOBが作成されたが、商品が割当られていない|
|2|Assigned|JOBに商品を割り当て、自動倉庫に指示を出した|
|3|Picking|自動倉庫が出庫を始めた|
|4|WaitOut|出庫が完了したが、商品が取り出されていない|
|5|Putaway|自動倉庫が入庫を始めた|
|6|Completed|JOBが完了した|
|7|Canceled|JOBがキャンセルされた|
|8|Recovering|商品の再割当を待っている|
|9|Aborted|再割当可能な商品が存在せず、JOBが成立しなくなった|

## JOB状態遷移

JOBの状態は、サーバー基準で遷移するものと、外部からの指示・報告を受けて遷移するものがある

- サーバー基準：Assigned、Recovering、Aborted
- スマホ指示：Unassigned、Canceled
- 自動倉庫報告：Picking、WaitOut、Putaway、Completed

### 出庫JOB

``` mermaid
stateDiagram-v2

[*] --> Unassigned : 出庫依頼

Unassigned --> Assigned : 商品割当

Assigned --> Picking : initiated
Assigned --> Recovering : 異常

Picking --> WaitOut : completed
Picking --> Recovering : 異常

WaitOut --> Completed : removed
WaitOut --> Recovering : 異常

Recovering --> Assigned : 商品再割当

Unassigned --> Aborted : 復旧不可
Recovering --> Aborted : 復旧不可

Unassigned --> Canceled : キャンセルされた
Recovering --> Canceled : キャンセルされた
Assigned --> Canceled : キャンセルされた

Canceled --> [*]
Completed --> [*]
Aborted --> [*]
```

### 入庫JOB

``` mermaid
stateDiagram-v2

[*] --> Assigned : 入庫依頼

Assigned --> Putaway : initiated

Putaway --> Completed : removed

Assigned --> Aborted : 異常
Putaway --> Aborted : 異常

Completed --> [*]
Aborted --> [*]
```

### サーバー再起動時

``` mermaid
stateDiagram-v2

[*] --> Unassigned : 再起動
[*] --> Assigned : 再起動
[*] --> Picking : 再起動
[*] --> WaitOut : 再起動
[*] --> Recovering : 再起動
[*] --> Putaway : 再起動

[*] --> Completed : 再起動
[*] --> Aborted : 再起動
[*] --> Canceled : 再起動

Unassigned --> Aborted: 初期化
Assigned --> Aborted: 初期化
Picking --> Aborted: 初期化
WaitOut --> Aborted: 初期化
Recovering --> Aborted: 初期化
Putaway --> Aborted: 初期化

Completed --> [*]
Aborted --> [*]
Canceled --> [*]
```

## タイムアウト

- JOBは、以下の状態のとき、時限をもつ
  - Unassigned：出庫のみ、JOBが作成されたが、商品が割り当てられていない
  - Assigned：JOBに商品が割り当てられて、自動倉庫に配信した
  - Picking：自動倉庫が出庫作業を開始した
  - WaitOut：出庫のみ、出庫動作が完了し人搬送を待っている
  - Putaway：自動倉庫が入庫作業を開始した
  - Recovering：自動倉庫に異常が発生し、JOBが中断された
- サーバーは、定時間ごとに上記状態にあるJOBの経過時間を確認し、超過を確認したJOBを異常とみなす

### 設定時間

- 今回は、デモ用に短めに設定する
- appsettings.jsonを利用して設定する

|状態|タイムアウト時間(s)|
|:---|:---|
|Unassigned|30|
|Assigned|15|
|Picking|30|
|WaitOut|30|
|Putaway|30|
|Recovering|60|

読み方：この状態のときに、○○秒経過したらタイムアウト扱いにする

## 異常発生時

商品の搬送中に異常が発生したときは、JOBを存続させるために、商品を再割当を行う。

### 異常の定義

- タイムアウト : 規定時間内に状態が遷移しなかった
- アラーム報告 : JOBを配信した装置からアラーム報告があった
- オンライン要求 : JOBを配信した装置からオンライン報告があった -> アラーム報告なしで再起動したとみなす

### 再割当ロジック

JOBの中断
1. （作業中の異常発生）
2. トランザクション開始
3. JOB状態ごとの商品の扱いを踏襲し、商品の在庫状態を変更する
4. 対象自動倉庫をオフラインにする
5. JOBの商品ID、装置IDをNULLに変え、JOB状態をRecoveringに変える
6. コミット

JOBの復旧
1. （上記処理のあと続いて実行）
2. トランザクション開始
3. 同一品種の在庫を、在庫情報から検索する
4. 割当可能は商品があれば
    - 商品をReservedにする
    - JOB.item_id / equipment_id を設定する
    - JOB状態を Assigned にする
5. 割当可能は商品がなければ
    - 在庫情報がない -> Aborted
    - 在庫はあるが、現在作業ができない -> Recoveringのまま
6. コミット

JOBの配信
1. AssignedのJOBをプッシュ配信する

### 異常時の商品の取り扱い

- Assigned以前の中断 → 商品を保管中に戻す
- Picking以降の中断 → 商品を在庫から除外/削除する

### Recoveringの扱い

- Recovering状態で、タイムアウトした場合は、復旧見込みなしとして、Abortedに遷移する
